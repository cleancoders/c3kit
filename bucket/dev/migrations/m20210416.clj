(ns migrations.m20210416
  (:require [c3kit.bucket.migrate :as m]))

(defn migrate
      "A description of the migration"
      []
      ;(m/rename :session/update-at :session/updated-at)
      ;(m/remove :session/key)
      )
